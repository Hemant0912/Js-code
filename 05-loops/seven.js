// reduce 

const myNums = [1, 2, 3]

const myTotal = myNums.reduce( (acc , curr) => acc+curr , 0 )
console.log(myTotal);

// accumulator value is given 0 --  "" curr, 0 ) ""
// we can change that value also

// current value is 1 for 0 
// cuurent value is taken from the array